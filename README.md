# eazfuscator-vs-ai

Tested against Eazfuscator's own private VM (their DLLs) - v2025.3

This repository combines:

- `EazFixer`: the Eazfuscator cleanup CLI.
- `EazDevirt`: the vendored and extended devirtualizer library used by EazFixer.
- `docs/`: current reverse-engineering notes, target-specific findings, and generated-artifact hashes.

The latest target-focused output is documented in
`docs/target-dnspy-readable-artifacts.md`.
